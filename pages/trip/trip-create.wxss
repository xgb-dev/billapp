.container {
  min-height: 100vh;
  background-color: var(--bg-page);
  padding: 24rpx 28rpx calc(140rpx + env(safe-area-inset-bottom));
}

.form-card {
  background-color: var(--card-bg);
  border-radius: var(--radius-lg);
  padding: 30rpx 28rpx;
  box-shadow: var(--shadow-md);
  border: 1rpx solid var(--border-light);
  margin-bottom: 24rpx;
}

.card-title-row {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 24rpx;
  padding-bottom: 16rpx;
  border-bottom: 1rpx solid var(--border-light);
}

.card-title {
  font-size: 30rpx;
  font-weight: 700;
  color: var(--text-main);
}

.card-tip {
  font-size: 22rpx;
  color: var(--text-muted);
}

.form-item {
  display: flex;
  flex-direction: column;
  padding: 12rpx 0;
}

.item-label {
  font-size: 26rpx;
  color: var(--text-sub);
  font-weight: 600;
  margin-bottom: 12rpx;
}

.form-input {
  height: 88rpx;
  min-height: 88rpx;
  line-height: 88rpx;
  font-size: 28rpx;
  color: var(--text-main);
  background-color: #F8FAFC;
  padding: 0 24rpx;
  border-radius: var(--radius-md);
  border: 1rpx solid #E2E8F0;
  box-sizing: border-box;
}

.input-placeholder {
  color: var(--text-muted);
  font-size: 28rpx;
}

.divider {
  height: 1rpx;
  background-color: var(--border-light);
  margin: 16rpx 0;
}

.date-pickers-wrap {
  display: flex;
  align-items: center;
  gap: 16rpx;
}

.date-btn {
  background-color: #F8FAFC;
  height: 84rpx;
  line-height: 84rpx;
  padding: 0 28rpx;
  border-radius: var(--radius-md);
  border: 1rpx solid #E2E8F0;
  font-size: 26rpx;
  color: var(--text-main);
  font-weight: 500;
  box-sizing: border-box;
  display: flex;
  align-items: center;
}

.date-sep {
  font-size: 26rpx;
  color: var(--text-muted);
}

/* 成员 Chip 气泡 */
.members-chips-wrap {
  display: flex;
  flex-wrap: wrap;
  gap: 16rpx;
  margin-bottom: 24rpx;
}

.member-chip {
  display: flex;
  align-items: center;
  background-color: #F1F5F9;
  padding: 12rpx 24rpx;
  border-radius: var(--radius-full);
  border: 1rpx solid #E2E8F0;
}

.member-chip.me {
  background-color: var(--primary-light);
  border-color: rgba(16, 185, 129, 0.3);
}

.member-chip.me .member-name {
  color: var(--primary);
  font-weight: 700;
}

.member-name {
  font-size: 26rpx;
  color: var(--text-main);
}

.remove-btn {
  margin-left: 12rpx;
  font-size: 32rpx;
  color: var(--text-muted);
  line-height: 1;
}

.add-member-bar {
  display: flex;
  gap: 16rpx;
}

.add-member-input {
  flex: 1;
  height: 84rpx;
  min-height: 84rpx;
  line-height: 84rpx;
  font-size: 26rpx;
  background-color: #F8FAFC;
  padding: 0 28rpx;
  border-radius: var(--radius-full);
  border: 1rpx solid #E2E8F0;
  box-sizing: border-box;
}

.add-btn {
  background: var(--primary);
  color: #FFFFFF;
  font-size: 26rpx;
  font-weight: 600;
  height: 84rpx;
  line-height: 84rpx;
  padding: 0 32rpx;
  border-radius: var(--radius-full);
  display: flex;
  align-items: center;
  justify-content: center;
  box-sizing: border-box;
}

/* 模板选择列表 */
.templates-list {
  display: flex;
  flex-direction: column;
  gap: 18rpx;
}

.tpl-card {
  display: flex;
  align-items: center;
  padding: 24rpx 20rpx;
  border-radius: var(--radius-md);
  border: 2rpx solid #E2E8F0;
  background-color: #FFFFFF;
  transition: all 0.2s ease;
}

.tpl-card.active {
  border-color: var(--primary);
  background-color: var(--primary-light);
}

.tpl-radio {
  width: 36rpx;
  height: 36rpx;
  border-radius: 50%;
  border: 2rpx solid #CBD5E1;
  display: flex;
  align-items: center;
  justify-content: center;
  margin-right: 18rpx;
}

.tpl-card.active .tpl-radio {
  border-color: var(--primary);
}

.radio-inner {
  width: 20rpx;
  height: 20rpx;
  border-radius: 50%;
  background-color: var(--primary);
}

.tpl-info {
  flex: 1;
  display: flex;
  flex-direction: column;
}

.tpl-name {
  font-size: 28rpx;
  font-weight: 700;
  color: var(--text-main);
  margin-bottom: 6rpx;
}

.tpl-desc {
  font-size: 22rpx;
  color: var(--text-muted);
}

/* 底部固定栏 */
.bottom-bar {
  position: fixed;
  bottom: 0;
  left: 0;
  right: 0;
  background: rgba(255, 255, 255, 0.95);
  backdrop-filter: blur(12px);
  padding: 20rpx 32rpx calc(24rpx + env(safe-area-inset-bottom));
  border-top: 1rpx solid var(--border-light);
  z-index: 100;
}

.submit-btn {
  width: 100%;
  height: 96rpx;
  line-height: 96rpx;
  border-radius: var(--radius-full);
  background: linear-gradient(135deg, #10B981 0%, #059669 100%);
  color: #FFFFFF;
  font-size: 32rpx;
  font-weight: 700;
  box-shadow: var(--shadow-lg);
}

/* 队长专属气泡样式 */
.member-chip.leader-chip {
  background: linear-gradient(135deg, #FEF3C7 0%, #FDE68A 100%);
  border-color: #F59E0B;
  box-shadow: 0 4rpx 10rpx rgba(245, 158, 11, 0.15);
  cursor: pointer;
}

.leader-badge {
  font-size: 20rpx;
  font-weight: 700;
  color: #B45309;
  background: rgba(255, 255, 255, 0.8);
  padding: 2rpx 10rpx;
  border-radius: 8rpx;
  margin-right: 8rpx;
}

.leader-chip .member-name {
  color: #92400E;
  font-weight: 700;
}

.edit-icon {
  margin-left: 8rpx;
  font-size: 24rpx;
  color: #D97706;
}

/* 弹窗样式 */
.modal-mask {
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background: rgba(15, 23, 42, 0.6);
  backdrop-filter: blur(4px);
  z-index: 1000;
  display: flex;
  align-items: center;
  justify-content: center;
  padding: 40rpx;
  box-sizing: border-box;
}

.modal-box {
  background: #FFFFFF;
  border-radius: 28rpx;
  width: 100%;
  max-width: 600rpx;
  padding: 40rpx 32rpx;
  box-sizing: border-box;
  box-shadow: 0 20rpx 40rpx rgba(15, 23, 42, 0.2);
}

.modal-title {
  font-size: 32rpx;
  font-weight: 700;
  color: #0F172A;
  text-align: center;
  margin-bottom: 8rpx;
}

.modal-sub {
  font-size: 24rpx;
  color: #64748B;
  text-align: center;
  margin-bottom: 28rpx;
}

.modal-field {
  margin-bottom: 24rpx;
}

.modal-input {
  background: #F8FAFC;
  border: 1rpx solid #CBD5E1;
  border-radius: 16rpx;
  height: 84rpx;
  min-height: 84rpx;
  line-height: 84rpx;
  padding: 0 24rpx;
  font-size: 28rpx;
  color: #0F172A;
  box-sizing: border-box;
  width: 100%;
}

.modal-btns {
  display: flex;
  gap: 16rpx;
  margin-top: 32rpx;
}

.m-btn {
  flex: 1;
  height: 80rpx;
  line-height: 80rpx;
  text-align: center;
  font-size: 28rpx;
  font-weight: 600;
  border-radius: 20rpx;
  border: none;
}

.m-btn.cancel {
  background: #F1F5F9;
  color: #64748B;
}

.m-btn.confirm {
  background: linear-gradient(135deg, #10B981 0%, #059669 100%);
  color: #FFFFFF;
}


